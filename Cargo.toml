[package]
name = "t212-client"
version = "0.1.0"
edition = "2021"
rust-version = "1.83"

# command will be 't212', even though the crate is t212-client
[[bin]]
name = "t212"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }