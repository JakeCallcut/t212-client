[package]
name = "t212client"
version = "0.1.0"
edition = "2021"
rust-version = "1.83"

[lib]
# Must match the #[pymodule] fn name in src/lib.rs.
name = "_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["abi3-py39", "extension-module"] }
